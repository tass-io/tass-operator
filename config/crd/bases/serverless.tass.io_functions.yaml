
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: functions.serverless.tass.io
spec:
  group: serverless.tass.io
  names:
    kind: Function
    listKind: FunctionList
    plural: functions
    singular: function
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: Function is the Schema for the functions API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: FunctionSpec defines the desired state of Function
          properties:
            environment:
              description: Environment represents the language environment of the
                code segments The scheduler wil then launch the corresponding language
                environment
              enum:
              - Golang
              - Python
              - JavaScript
              type: string
            resource:
              description: Resource claims the resource provisioning for Function
                process It now contains cpu and memory
              properties:
                cpu:
                  description: CPU, in cores. (500m = .5 cores)
                  type: string
                memory:
                  description: Memory, in bytes. (500Gi = 500GiB = 500 * 1024 * 1024
                    * 1024)
                  type: string
              required:
              - cpu
              - memory
              type: object
          required:
          - environment
          - resource
          type: object
        status:
          description: FunctionStatus defines the observed state of Function
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
